
5장_
다음 조건을 참고하여 손실함수(mse)를 완성해라.
```text
1. t_p와 t_c는 동일 shape의 텐서이다.
2. MSE는 (t_p - t_c)^2의 평균(mean) 을 반환해야 한다.
```
<pre><code class="language-python">
def loss_fn(t_p, t_c):
	squared_diffs = <string>(t_p - t_c)**2</string>
	return squared_diffs.mean()
</code></pre>

---

