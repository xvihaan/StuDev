
4장_
다음 코드에서 구식의 차원 레이아웃에서 새로운 차원으로 변경하여 적절한 레이아웃을 얻는 메소드는?
<pre><code class="language-python">
img = torch.numpy(img_arr)
out = img.<strong>permute</strong>(2, 0, 1)
</code></pre>
Hint: 텐서 레벨에서 크기와 스트라이드 정보만 변경(복사본X)

---

4장_
다음은 이미지 배치 텐서 batch가 주어졌을 때, 각 **채널별로 평균을 0, 표준편차를 1로 정규화**하는 코드이다. 배치 텐서 batch는 (N, C, H, W) 형태를 가지고 있으며, float로 변환되고 [0, 1] 범위로 스케일링된 상태이다.
아래 코드의 빈칸을 채워 정규화를 올바르게 구현하시오
<pre><code class="language-python">
n_channels = batch.shape[1]
for c in range(n_channels):
	mean = torch.mean(batch[:, c])
	std = torch.std(batch[:, c])
	batch[:, c] = <strong>(batch[:, c] - mean) / std</strong>
</code></pre>

---

4장_
PyTorch에서 다음과 같은 정수 레이블 텐서 target이 존재할 때, scatter_()를 사용해 **10차원 원-핫 인코딩 텐서**를 생성하려 한다.
다음 코드의 빈칸 (①), (②) 을 올바르게 채워보시오.
<pre><code class="language-python">
target_onehot = ①   # zeros로 raw의 개수(shape[0])은 10개로 채운다는 뜻
target_onehot.scatter_( ② )  # 벡터(스칼라 형태로 된 레이블을 담긴 구조)를 행렬화 하기 위해
</code></pre>
?
```python
① torch.zeros(target.shape[0], 10
② (1, target.unsqueeze(1), 1.0)
```

---
